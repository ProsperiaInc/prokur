import React from 'react';

import CustomIcon from './CustomIcon';

export default {
  title: 'Prokur/Components/CustomIcon',
  component: CustomIcon,
};

const Template = (args: any) => <CustomIcon {...args} />;

export const Default = Template.bind({});
