import React from 'react';

import Attachment from './Attachment';

export default {
  title: 'Prokur/Components/Attachment',
  component: Attachment,
};

const Template = (args) => <Attachment {...args} />;

export const Default = Template.bind({});
